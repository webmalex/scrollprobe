minimum_pre_commit_version: "4.0.0"
default_install_hook_types: [commit-msg]

repos:
  - repo: local
    hooks:
      - id: commit-message
        name: validate commit message
        entry: scripts/check-commit-message
        language: script
        stages: [commit-msg]
        always_run: true
